/**
 * Status colors component interface data Props.
 *
 * @export
 * @interface StatusColorsDataProps
 * @typedef {StatusColorsDataProps}
 */
export interface StatusColorsDataProps {
  id: number;
  title: string;
  bg_color: string;
}
